import React, { Fragment } from 'react';

const Header = ({active}) => {
    return (
        <Fragment>
            <div className="header">
                <h3>Icube Training Result</h3>
            </div>
        </Fragment>
    );
};

export default Header;